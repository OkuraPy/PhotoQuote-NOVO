// v2 reuses the configured Supabase client (URL + anon key from app config).
// SecureStore for the session is planned for the security phase (Fase 5).
export { supabase } from '../../services/supabase';
